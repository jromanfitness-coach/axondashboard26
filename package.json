{
  "name": "tifton-fitness-workout-dashboard",
  "version": "30.0.0",
  "private": true,
  "description": "Axon workout dashboard with resilient staff/client login and optional Netlify Blobs live sync.",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "verify": "node scripts/verify-netlify-build.cjs",
    "build": "npm ci --omit=dev --no-audit --no-fund && npm run verify"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.9"
  }
}
