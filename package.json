{
  "name": "tifton-fitness-workout-dashboard",
  "version": "31.0.0",
  "private": true,
  "description": "Axon workout dashboard with server-side staff/client PIN login and optional Netlify Blobs sync.",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "verify": "node scripts/verify-netlify-build.cjs",
    "build": "npm ci --omit=dev --no-audit --no-fund && npm run verify"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.9"
  }
}
