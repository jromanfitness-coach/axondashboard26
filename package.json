{
  "name": "tifton-fitness-workout-dashboard",
  "version": "29.0.0",
  "private": true,
  "description": "Tifton Fitness staff dashboard with separate universal client portal and Netlify Blobs sync using automatic Function credentials.",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "verify": "node scripts/verify-netlify-build.cjs",
    "build": "npm ci --omit=dev --no-audit --no-fund && npm run verify"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.9"
  }
}
