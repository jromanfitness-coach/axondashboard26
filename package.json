{
  "name": "tifton-fitness-workout-dashboard",
  "version": "26.0.0",
  "private": true,
  "description": "Tifton Fitness admin workout dashboard with separate client PIN portal and Netlify Blobs sync.",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "verify": "node scripts/verify-netlify-build.cjs",
    "build": "npm install --omit=dev --no-audit --no-fund && npm run verify"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.9"
  }
}
